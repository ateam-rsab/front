define(['initialize'], function(initialize) {
    'use strict';
    initialize.controller('TransferPasienInternalCtrl', ['$rootScope', '$scope', 'ModelItem', '$state',
    	function($rootScope, $scope, ModelItem, $state) {
	        //$scope.title = "transfer Pasien Internal";
    }]);
});
