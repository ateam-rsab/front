define(['initialize'], function(initialize) {
    'use strict';
    initialize.controller('TouchScreenCtrl', ['$scope', 'ModelItem', '$rootScope', function($scope, modelItem, $rootScope) {
       
    }]);
}); 